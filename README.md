# AWS SQS and SQS Dead Letter Queue

Configuration in this directory creates set of SQS Queue & Dead Letter Queue resources.

## Usage

To run this example you need to execute:

```bash
$ terraform init
$ terraform plan
$ terraform apply
```